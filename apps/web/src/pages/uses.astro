---
/**
 * /uses moved into the colophon (see colophon.astro #uses). Permanent redirect
 * so old links + bookmarks still land on the content. SSR (not prerendered) so
 * the 301 status is real, not a meta-refresh.
 */
export const prerender = false;
return Astro.redirect('/colophon#uses', 301);
---
